# 直接依赖（人类可读）；全量锁定见 requirements.lock（pip-compile 生成）
# 重新生成：.venv/bin/python -m piptools compile requirements.txt -o requirements.lock
django>=6.0,<7
channels
daphne
twisted[http2,tls]
whitenoise
jinja2
lxml
markdown
pymdown-extensions
pygments
pyquery
pyyaml
requests
rich
types-markdown
types-pyyaml
types-requests
